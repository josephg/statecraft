// // This is a proxy store, used to proxy to another store. The store we proxy
// // to can be changed at runtime.

// import * as I from '../types/interfaces'

// // The reason for this store is so we can implement reconnections.
// const proxy = () => {
//   const innerStore: I.Store | null = null

//   const setStore = (store: I.Store | null) => {

//   }

//   const store: I.Store = {

//   }

//   return [setStore, store]
// }
